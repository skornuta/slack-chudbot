# Slack-chudbot

A simple Slack bot with multiple commands. 

## Setup
1. Install dependencies:
```bash
pip install -r requirements.txt```

2. Copy the `.env.example` file to `.env` and fill in the values.

3. Run the bot:
```python index.js```
